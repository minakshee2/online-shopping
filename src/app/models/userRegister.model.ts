export interface IuserRegister {
  userId: string; // Unique identifier for the user
  name: string; // User's name
  userLogin: string; // User's contact (mobile or email)
  password: string; // User's password
}
